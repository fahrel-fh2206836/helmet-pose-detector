# Helmet Pose Classification
Detecting and classifying whether the person wearing the helmet is directly looking at the camera or not.

# Note
📱Mobile App is currently being developed for now you can test and run the model for testing using the file `helmet-model-test.py` which is a simple OpenCV interface integrated with the model.
